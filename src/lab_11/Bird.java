package lab_11;

import java.security.SecureRandom;

public class Bird extends Animals{

    public Bird(String name, int speed, boolean isWings) {
        super(name, speed, isWings);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
